sync:
  defaults:
    ignore:
      vcs: true
      paths:
        - .DS_Store
        - ._*
        - .solargraph.yml
        - .vscode/
        - \*.dwarf
        - \*.egg-info/
        - \*.o
        - \*.py[cod]
        - \*.sw[a-p]
        - \*~
        - __pycache__/
        - mutagen.yml*
        - secrets/
  code:
    alpha: .
    beta: docker://docker-alpine_mutagen/mnt/code
    mode: two-way-resolved
